{
  "name": "spec2mock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Turn an OpenAPI spec into a deployable WireMock mock (k8s + Docker) — entirely in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/test-generate.js && node scripts/test-artifacts.js && node scripts/test-convert.js"
  },
  "dependencies": {
    "file-saver": "^2.0.5",
    "js-yaml": "^4.1.0",
    "jszip": "^3.10.1",
    "openapi-sampler": "^1.5.1"
  },
  "devDependencies": {
    "swagger2openapi": "^7.0.8",
    "vite": "^5.4.11"
  }
}
